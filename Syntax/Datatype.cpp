#include<bits/stdc++.h>   //library fro everythingh
using namespace std;

int main() {
    int x=0;   //x will only store integer

    long x=15;
    long long x=150000000000;     //for very long value

    float x=5.6 ; ///for fraction value

    string s;  //for string
    cin>>s;   //for taking input one word
    cout<<s;


    string str;
    getline(cin,str);  //for taking multiple word in single line
    cout<<str;


    char ch;   //for character
    char ch='g';   //char is denoted by generally ' ' and string " "
    cout<<ch;

    return 0;
}